name: Deploy → Dev (dev.maboximmo.fr)

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  deploy:
    name: Upload sur Hostinger — Dev
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Déploiement FTP vers public_html/dev
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.maboximmo.fr
          port: 21
          protocol: ftp
          username: u630423897.maboximmo
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./public_html/
          server-dir: /dev/
          state-name: .ftp-deploy-sync-v2
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.DS_Store
            **/uploads/**
            **/tcpdf_cache/**
            **/config/smtp_local.php
            **/config/ubiflow_credentials.local.php
            **/db_config.php
